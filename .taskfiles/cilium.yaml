---
version: "3"

tasks:
  
  status:
    cmds:
      - cilium clustermesh status --context k0s
      - cilium clustermesh status --context k8s

  setup-cilium-mesh:
    cmds:
      - cilium hubble enable --create-ca --context k0s
      #- cilium clustermesh status # connect --context k0s --destination-context k8s
