---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: &appname vector-aggregator
spec:
  endpoints:
    - interval: 2m
      path: /metrics
      port: metrics
      scheme: http
      scrapeTimeout: 10s
  selector:
    matchLabels:
      app.kubernetes.io/name: *appname
