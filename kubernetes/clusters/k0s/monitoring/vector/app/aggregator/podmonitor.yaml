---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: &appname vector-aggregator
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: *appname
  podMetricsEndpoints:
    - port: metrics