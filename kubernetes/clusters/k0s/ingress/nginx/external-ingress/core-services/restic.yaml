---
apiVersion: v1
kind: Service
metadata:
  name: restic
  namespace: ingress
spec:
  ports:
  - name: http
    port: 443
    protocol: TCP
    targetPort: 443
  clusterIP: None
  type: ClusterIP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: restic
  namespace: ingress
subsets:
- addresses:
  - ip: "192.168.8.2"
  ports:
  - name: http
    port: 443
    protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: restic
  namespace: ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
spec:
  ingressClassName: nginx
  rules:
  - host: "restic.feelingsmachine.com"
    http:
      paths:
      - backend:
          service:
            name: restic
            port:
              number: 443
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - "restic.feelingsmachine.com"
