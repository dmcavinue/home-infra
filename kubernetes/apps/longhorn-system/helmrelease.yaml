---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  releaseName: longhorn
  interval: 5m
  chart:
    spec:
      chart: longhorn
      version: 1.4.1
      sourceRef:
        kind: HelmRepository
        name: longhorn
        namespace: flux-system
      interval: 5m
  values:
    csi:
      kubeletRootDir: /var/lib/kubelet
    persistence:
      defaultClass: false
      recurringJobSelector:
        enable: false
    defaultSettings:
      backupTarget: "nfs://192.168.10.2:/volume1/pv/production/longhorn"
    ingress:
      enabled: false
