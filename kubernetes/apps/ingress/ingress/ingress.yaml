---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: ingress
spec:
  ingressClassName: cilium
  tls:
  - hosts:
    - "*.${DOMAIN}"
    secretName: "${DOMAIN/./-}-tls"
  rules:
  - host: "*.${DOMAIN}"
