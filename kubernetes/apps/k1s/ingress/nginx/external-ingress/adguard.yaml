---
apiVersion: v1
kind: Service
metadata:
  name: adguard
  namespace: ingress
  annotations:
    io.cilium/global-service: "true"
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  clusterIP: None
  type: ClusterIP
