---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: db-immich
  namespace: database
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: db-immich
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /database/cloudnative-pg
        property: POSTGRES_IMMICH_USER
    - secretKey: password
      remoteRef:
        key: /database/cloudnative-pg
        property: POSTGRES_IMMICH_PASS
