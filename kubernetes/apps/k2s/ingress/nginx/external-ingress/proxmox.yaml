---
apiVersion: v1
kind: Service
metadata:
  name: proxmox
  namespace: ingress
  annotations:
    io.cilium/global-service: "true"
spec:
  ports:
  - name: http
    port: 8006
    protocol: TCP
    targetPort: 8006
  clusterIP: None
  type: ClusterIP
