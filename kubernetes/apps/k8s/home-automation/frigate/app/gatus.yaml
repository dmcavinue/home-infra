---
apiVersion: v1
kind: ConfigMap
metadata:
  name: frigate-gatus
  namespace: home-automation
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: frigate
        group: home-automation
        url: https://frigate.${DOMAIN}
        interval: 1m
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: slack
