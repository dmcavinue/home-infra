---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zwavejs-gatus
  namespace: home-automation
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: zwavejs
        group: home-automation
        url: https://zwavejs.${DOMAIN}
        interval: 1m
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: slack
