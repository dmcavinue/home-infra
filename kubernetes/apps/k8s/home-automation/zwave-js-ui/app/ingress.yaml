apiVersion: networking.${ENVIRONMENT}.io/v1
kind: Ingress
metadata:
  name: zwavejs
  namespace: home-automation
  annotations:
    nginx.ingress.kubernetes.io/auth-url: https://oauth.nucstack.com/oauth2/auth
    nginx.ingress.kubernetes.io/auth-signin: https://oauth.nucstack.com/oauth2/start
spec:
  ingressClassName: nginx
  rules:
  - host: "zwavejs.nucstack.com"
    http:
      paths:
      - backend:
          service:
            name: zwave-js-ui
            port:
              number: 8091
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - "zwavejs.nucstack.com"
