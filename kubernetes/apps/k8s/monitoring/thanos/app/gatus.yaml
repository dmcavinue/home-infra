---
apiVersion: v1
kind: ConfigMap
metadata:
  name: thanos-gatus
  namespace: monitoring
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: thanos
        group: monitoring
        url: https://thanos.${DOMAIN}
        interval: 1m
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: slack
