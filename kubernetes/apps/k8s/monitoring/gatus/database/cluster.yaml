---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: gatus
  namespace: monitoring
  annotations:
    kyverno.io/ignore: "true"
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:14.8-7
  inheritedMetadata:
    labels:
      kube-image-keeper.enix.io/image-caching-policy: ignore
  primaryUpdateStrategy: unsupervised
  bootstrap:
    # initdb:
    #   database: gatus
    #   owner: gatus
    #   secret:
    #     name: gatus-database-user
    recovery:
      source: &previousCluster gatus-1
  storage:
    size: 5Gi
    storageClass: local-path
  superuserSecret:
    name: gatus-database-superuser
  postgresql:
    parameters:
      max_connections: "600"
      shared_buffers: 512MB
  monitoring:
    enablePodMonitor: true
  backup:
    retentionPolicy: 14d
    barmanObjectStore:
      wal:
        compression: bzip2
        maxParallel: 8
      destinationPath: s3://db-backup/gatus
      endpointURL: "https://minio.${LOCAL_DOMAIN}"
      serverName: &currentCluster gatus-2
      s3Credentials:
        accessKeyId:
          name: gatus-database-superuser
          key: access-key
        secretAccessKey:
          name: gatus-database-superuser
          key: secret
  externalClusters:
  - name: *previousCluster
    barmanObjectStore:
      destinationPath: s3://db-backup/gatus
      endpointURL: "https://minio.${LOCAL_DOMAIN}"
      s3Credentials:
        accessKeyId:
          name: gatus-database-superuser
          key: access-key
        secretAccessKey:
          name: gatus-database-superuser
          key: secret