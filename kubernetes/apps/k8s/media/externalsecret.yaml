---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-auth
  namespace: media
---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-kv-backend
  namespace: media
spec:
  provider:
    vault:
      server: "https://vault.logfold.com"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes/k8s"
          role: "media"
          serviceAccountRef:
            name: "vault-auth"
