---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: coder-database-superuser
  namespace: 
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: coder-database-superuser
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /development/coder
        property: CODER_POSTGRES_SUPER_USER
    - secretKey: password
      remoteRef:
        key: /development/coder
        property: CODER_POSTGRES_SUPER_PASSWORD
    - secretKey: access-key
      remoteRef:
        key: /development/coder
        property: CODER_DB_BACKUP_ACCESS_KEY
    - secretKey: secret
      remoteRef:
        key: /development/coder
        property: CODER_DB_BACKUP_SECRET
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: coder-database-user
  namespace: development
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: coder-database-user
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /development/coder
        property: CODER_POSTGRES_USER
    - secretKey: password
      remoteRef:
        key: /development/coder
        property: CODER_POSTGRES_PASSWORD