---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: backstage-database-superuser
  namespace: development
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: backstage-database-superuser
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /development/backstage
        property: BACKSTAGE_POSTGRES_SUPER_USER
    - secretKey: password
      remoteRef:
        key: /development/backstage
        property: BACKSTAGE_POSTGRES_SUPER_PASSWORD
    - secretKey: access-key
      remoteRef:
        key: /development/backstage
        property: BACKSTAGE_DB_BACKUP_ACCESS_KEY
    - secretKey: secret
      remoteRef:
        key: /development/backstage
        property: BACKSTAGE_DB_BACKUP_SECRET
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: backstage-database-user
  namespace: development
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: backstage-database-user
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /development/backstage
        property: BACKSTAGE_POSTGRES_USER
    - secretKey: password
      remoteRef:
        key: /development/backstage
        property: BACKSTAGE_POSTGRES_PASSWORD