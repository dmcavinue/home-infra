apiVersion: rbac.authorization.${ENVIRONMENT}.io/v1
kind: ClusterRole
metadata:
  name: weave-sa-impersonator
rules:
- apiGroups: [""]
  resources: ["users"]
  verbs: ["impersonate"]
---
apiVersion: rbac.authorization.${ENVIRONMENT}.io/v1
kind: ClusterRoleBinding
metadata:
  name: weave-sa-impersonator
roleRef:
  apiGroup: rbac.authorization.${ENVIRONMENT}.io
  kind: ClusterRole
  name: weave-sa-impersonator
subjects:
- apiGroup: rbac.authorization.${ENVIRONMENT}.io
  kind: User
  name: dan@mcavinue.io
---
apiVersion: rbac.authorization.${ENVIRONMENT}.io/v1
kind: ClusterRoleBinding
metadata:
  name: weave-cluster-admin
roleRef:
  apiGroup: rbac.authorization.${ENVIRONMENT}.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- apiGroup: rbac.authorization.${ENVIRONMENT}.io
  kind: User
  name: dan@mcavinue.io