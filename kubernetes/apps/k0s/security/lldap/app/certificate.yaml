---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "lldap-${DOMAIN/./-}"
  namespace: security
spec:
  secretName: "lldap-${DOMAIN/./-}-tls"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "lldap.${DOMAIN}"
  dnsNames:
  - "lldap.${DOMAIN}"
