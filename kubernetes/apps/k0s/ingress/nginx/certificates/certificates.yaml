---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "logfold-com"
  namespace: ingress
spec:
  secretName: "logfold-com-tls"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "${DOMAIN}"
  dnsNames:
  - "${DOMAIN}"
  - "*.${DOMAIN}"
