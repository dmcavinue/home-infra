---
apiVersion: v1
kind: Service
metadata:
  name: adguard
  namespace: ingress
spec:
  ports:
  - name: http
    port: 443
    protocol: TCP
    targetPort: 443
  clusterIP: None
  type: ClusterIP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: adguard
  namespace: ingress
subsets:
- addresses:
  - ip: "${INGRESS_ADGUARD_IP}"
  ports:
  - name: http
    port: 443
    protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard
  namespace: ingress
spec:
  ingressClassName: nginx
  rules:
  - host: "adguard.${DOMAIN}"
    http:
      paths:
      - backend:
          service:
            name: adguard
            port:
              number: 443
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - "adguard.${DOMAIN}"
