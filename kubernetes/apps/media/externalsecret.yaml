---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-auth
  namespace: media
---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-kv-backend
  namespace: media
spec:
  provider:
    vault:
      server: "https://vault.${LOCAL_DOMAIN}"
      path: "kv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes/${ENVIRONMENT}"
          role: "media"
          serviceAccountRef:
            name: "vault-auth"
