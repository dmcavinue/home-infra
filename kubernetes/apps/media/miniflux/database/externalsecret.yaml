---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: miniflux-database-superuser
  namespace: media
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: miniflux-database-superuser
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /media/miniflux
        property: MINIFLUX_POSTGRES_SUPER_USER
    - secretKey: password
      remoteRef:
        key: /media/miniflux
        property: MINIFLUX_POSTGRES_SUPER_PASSWORD
    - secretKey: access-key
      remoteRef:
        key: /media/miniflux
        property: MINIFLUX_DB_BACKUP_ACCESS_KEY
    - secretKey: secret
      remoteRef:
        key: /media/miniflux
        property: MINIFLUX_DB_BACKUP_SECRET
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: miniflux-database-user
  namespace: media
spec:
  secretStoreRef:
    name: vault-kv-backend
    kind: SecretStore
  target:
    name: miniflux-database-user
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: /media/miniflux
        property: MINIFLUX_POSTGRES_USER
    - secretKey: password
      remoteRef:
        key: /media/miniflux
        property: MINIFLUX_POSTGRES_PASSWORD
